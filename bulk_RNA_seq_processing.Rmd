#### Load libraries ---
library(clusterProfiler)
library(DESeq2)
library(dplyr)
library(ggplot2)
library(org.Hs.eg.db)
library(ragg)
library(readr)
library(Rsubread)
library(stringr)
library(tidyverse)
library(biomaRt)
library(purrr)
library(tibble)
library(ggrepel)

### Read in and align pair-end reads ---
reads1 <- list.files(path = "/Users/admin/Documents/Andrew/MF bulk RNA seq/", pattern = "R1_001.fastq.gz$", full.names = TRUE)
reads2 <- list.files(path = "/Users/admin/Documents/Andrew/MF bulk RNA seq/", pattern = "R2_001.fastq.gz$", full.names = TRUE)

# Align based on hg38 files (available online)
align(index = "hg38_index", 
      readfile1 = reads1, 
      readfile2 = reads2,
      type = "rna",
      output_format = "BAM", 
      nthreads = 8)

trimmed_dir <- "/Users/admin/Documents/Andrew/MF bulk RNA seq/"
bam.files <- list.files(trimmed_dir, pattern = "BAM$")
bam.files

# Compile and save feature Counts
fc <- featureCounts(bam.files, annot.inbuilt = "hg38", isPairedEnd = TRUE)
saveRDS(fc, "2025.12.11_fc.RDS")

### Process feature Counts ---
fc <- readRDS("2025.12.11_fc.RDS")

count_matrix <- fc$counts

sample_names <- c("14238-MF-0001_S1_L005_R1_001.fastq.gz.subread.BAM",
                  "14238-MF-0002_S1_L005_R1_001.fastq.gz.subread.BAM",
                  "14238-MF-0003_S1_L005_R1_001.fastq.gz.subread.BAM",
                  "14238-MF-0004_S1_L005_R1_001.fastq.gz.subread.BAM",
                  "14238-MF-0005_S1_L005_R1_001.fastq.gz.subread.BAM",
                  "14238-MF-0006_S1_L005_R1_001.fastq.gz.subread.BAM"
                  )

# Assign metadata
protocol <- factor(c('P301L,V337M DOX','P301L,V337M DOX','P301L,V337M DOX','P301L,V337M', 'P301L,V337M','P301L,V337M'))

# Create metadata dataframe
coldata <- data.frame(
  row.names = sample_names,
  protocol = protocol
)

dds <- DESeqDataSetFromMatrix(countData = fc$counts,
                              colData = coldata,
                              design = ~ protocol)
dds <- DESeq(dds)

# Map gene symbols
gene_symbols <- mapIds(
  org.Hs.eg.db,
  keys = rownames(dds),
  column = "SYMBOL",
  keytype = "ENTREZID",
  multiVals = "first"
)

# Attach gene symbols to the DESeqDataSet object
mcols(dds)$symbol <- gene_symbols

### RUN DEG, GO ---
dir.create("DE_results", showWarnings = FALSE)
dir.create("GO_results", showWarnings = FALSE)

# Define contrast
g1 <- "P301L,V337M DOX"
g2 <- "P301L,V337M"
contrast_name <- paste0(g1, "_vs_", g2)
message("=== Contrasting: ", contrast_name, " ===")

# Map gene symbols
entrez_ids <- rownames(dds)
gene_symbols <- mapIds(
  org.Hs.eg.db,
  keys = entrez_ids,
  keytype = "ENTREZID",
  column = "SYMBOL",
  multiVals = "first"
)

# DE analysis
res <- results(dds, contrast = c("protocol", g1, g2))
res <- lfcShrink(dds, contrast = c("protocol", g1, g2),
                 res = res, type = "ashr")

res_df <- as.data.frame(res)
res_df$symbol <- ifelse(
  is.na(gene_symbols[rownames(res_df)]),
  rownames(res_df),
  gene_symbols[rownames(res_df)]
)

clean <- res_df %>%
  filter(is.finite(log2FoldChange), is.finite(padj), !is.na(padj))

write.csv(clean,
          file = file.path("DE_results",
                           paste0(contrast_name, "_DEGs.csv")),
          row.names = TRUE)

# Split into up and down
up_genes   <- rownames(clean)[clean$padj < 0.05 & clean$log2FoldChange > 0.1]
down_genes <- rownames(clean)[clean$padj < 0.05 & clean$log2FoldChange < -0.1]
universe <- rownames(clean)

# Write function to run GO, and simplify terms for redundancy
run_GO <- function(genes, direction) {
  if (length(genes) < 5) {
    message("Too few ", direction, " genes for GO (", length(genes), ") — skipping")
    return(NULL)
  }
  
  # Full GO enrichment
  ego <- enrichGO(
    gene     = genes,
    universe = universe,
    OrgDb    = org.Hs.eg.db,
    keyType  = "ENTREZID",
    ont      = "BP",
    pAdjustMethod = "BH",
    qvalueCutoff  = 0.1,
    readable = TRUE
  )
  
  # Save full results
  write.csv(as.data.frame(ego),
            file = file.path("GO_results",
                             paste0(contrast_name, "_GO_", direction, "_BP.csv")),
            row.names = FALSE)
  
  # Simplify GO
  ego_simpl <- clusterProfiler::simplify(
    ego,
    cutoff = 0.5,
    by = "p.adjust",
    select_fun = min
  )
  
  # Save simplified results
  write.csv(as.data.frame(ego_simpl),
            file = file.path("GO_results",
                             paste0(contrast_name, "_GO_", direction, "_BP_simplified.csv")),
            row.names = FALSE)
  
  return(list(full = ego, simplified = ego_simpl))
}

# Run GO function
ego_up <- run_GO(up_genes, "UP")
ego_down <- run_GO(down_genes, "DOWN")
)

### Plot GO analysis and DEGs ---
# Read in files
go_down <- read_csv("GO_results/P301L,V337M DOX_vs_P301L,V337M_GO_DOWN_BP.csv")
go_up   <- read_csv("GO_results/P301L,V337M DOX_vs_P301L,V337M_GO_UP_BP.csv")
degs    <- read_csv("DE_results/P301L,V337M DOX_vs_P301L,V337M_DEGs.csv")

# Extract gene lists from FIRST ROW ONLY
down_genes <- str_split(go_down$geneID[1], "/", simplify = TRUE)
up_genes   <- str_split(go_up$geneID[1], "/", simplify = TRUE)

# Extract pathway descriptions from FIRST ROW ONLY
down_desc <- go_down$Description[1]
up_desc   <- go_up$Description[1]

# Create clean column names
down_colname <- paste0(down_desc, "_DOWN_pathway")
up_colname   <- paste0(up_desc, "_UP_pathway")

# Optional: make column names syntactically valid
down_colname <- make.names(down_colname)
up_colname   <- make.names(up_colname)

# Add pathway membership columns to DEG dataframe
degs <- degs %>%
  mutate(
    !!down_colname := symbol %in% down_genes,
    !!up_colname   := symbol %in% up_genes
  )

# Identify pathway columns dynamically
pathway_cols <- grep("_pathway$", colnames(degs), value = TRUE)
up_col   <- pathway_cols[grepl("_UP_", pathway_cols)]
down_col <- pathway_cols[grepl("_DOWN_", pathway_cols)]

# Create plotting category
degs_plot <- degs %>%
  mutate(
    pathway_status = case_when(
      !!sym(up_col)   ~ "UP pathway",
      !!sym(down_col) ~ "DOWN pathway",
      TRUE            ~ "Other"
    )
  )

# Select top 9 UP and DOWN genes by adjusted p-value
top_genes <- degs_plot %>%
  filter(!is.na(padj)) %>%
  mutate(direction = if_else(log2FoldChange > 0, "UP", "DOWN")) %>%
  group_by(direction) %>%
  slice_min(order_by = padj, n = 9, with_ties = FALSE) %>%
  ungroup()

# Volcano plot
volcano_plot <- ggplot(degs_plot, aes(x = log2FoldChange, y = -log10(padj))) +
  ## Fold change cutoff lines
  geom_vline(
    xintercept = c(-0.1, 0.1),
    linetype = "dashed",
    color = "black",
    linewidth = 0.4
  ) +

  # Background points
  geom_point(
    data = filter(degs_plot, pathway_status == "Other"),
    color = "grey70",
    alpha = 0.5,
    size = 1.5
  ) +

  # DOWN pathway points
  geom_point(
    data = filter(degs_plot, pathway_status == "DOWN pathway"),
    color = "blue",
    alpha = 0.5,
    size = 2
  ) +

  ## UP pathway points
  geom_point(
    data = filter(degs_plot, pathway_status == "UP pathway"),
    color = "red",
    alpha = 0.5,
    size = 2
  ) +

  # Labels: top 9 UP + top 9 DOWN
  geom_text_repel(
    data = top_genes,
    aes(label = symbol),
    size = 3,
    vjust = 0.5,
    hjust = 0.5
  ) + 
  scale_y_continuous(
  limits = c(0, NA),
  oob = scales::oob_keep
) +

  theme_classic(base_size = 14) +
  labs(
    x = "log2 Fold Change",
    y = "-log10 adjusted p-value"
  )
ggsave("VolcanoPlot_Publication.png", plot = volcano_plot, width = 6, height = 5, dpi = 300)


# Read GO UP file
go_up <- read.csv("GO_results/P301L,V337M DOX_vs_P301L,V337M_GO_UP_BP_simplified.csv")

# Select top 10 hits by p.adjust
top10_go_up <- go_up %>%
  filter(!is.na(p.adjust)) %>%
  arrange(p.adjust) %>%
  slice(1:10) %>%
  mutate(
    Description = factor(Description, levels = rev(Description)),
    TopHit = ifelse(row_number() == 1, "Top hit", "Other"),
    Alpha = ifelse(TopHit == "Top hit", 1, 0.5)
  )

# Lollipop plot
up_plot <- ggplot(top10_go_up, aes(y = Description)) +
  # Stems
  geom_segment(aes(x = 0, xend = -log10(p.adjust), yend = Description),
               color = "grey70") +

  # Points with fill mapped to TopHit
  geom_point(aes(x = -log10(p.adjust), size = Count, fill = TopHit),
             shape = 21, color = "black", stroke = 0.5, alpha = top10_go_up$Alpha) +

  scale_fill_manual(values = c("Top hit" = "red", "Other" = "grey70")) +
  scale_size_continuous(range = c(3,7)) +
  theme_classic(base_size = 14) +
  labs(
    x = "-log10 adjusted p-value",
    y = "GO Term",
    size = "Gene Count",
    fill = "Significance",
    title = "Top 10 DOX GO BP Terms"
  )

ggsave("up_GO.png", plot = up_plot, width = 8.2, height = 3, dpi = 300)


# Read GO DOWN file
go_down <- read.csv("GO_results/P301L,V337M DOX_vs_P301L,V337M_GO_DOWN_BP_simplified.csv")

# Select top 10 hits by p.adjust
top10_go_down <- go_down %>%
  filter(!is.na(p.adjust)) %>%
  arrange(p.adjust) %>%
  slice(1:10) %>%
  mutate(
    Description = factor(Description, levels = rev(Description)),
    TopHit = ifelse(row_number() == 1, "Top hit", "Other"),
    Alpha = ifelse(TopHit == "Top hit", 1, 0.5)
  )

# Lollipop plot
down_plot <- ggplot(top10_go_down, aes(y = Description)) +
  # Stems
  geom_segment(aes(x = 0, xend = -log10(p.adjust), yend = Description),
               color = "grey70") +

  # Points with fill mapped to TopHit
  geom_point(aes(x = -log10(p.adjust), size = Count, fill = TopHit),
             shape = 21, color = "black", stroke = 0.5, alpha = top10_go_down$Alpha) +

  scale_fill_manual(values = c("Top hit" = "blue", "Other" = "grey70")) +
  scale_size_continuous(range = c(3,7)) +
  theme_classic(base_size = 14) +
  labs(
    x = "-log10 adjusted p-value",
    y = "GO Term",
    size = "Gene Count",
    fill = "Significance",
    title = "Top 10 NO DOX GO BP Terms"
  )

ggsave("down_GO.png", plot = down_plot, width = 8, height = 3, dpi = 300)


### COMPUTE MODULE SCORES
# File paths
deg_file <- "DE_results/P301L,V337M DOX_vs_P301L,V337M_DEGs.csv"
modules_file <- "concensusModules.tsv"
rosmap_file <- "differentialExpressionSummary.tsv"

# Read in the data
deg_df <- read.csv(deg_file, header = TRUE, stringsAsFactors = FALSE)
modules_df <- read.delim(modules_file, header = TRUE, stringsAsFactors = FALSE)
rosmap_df <- read.delim(rosmap_file, header = TRUE, stringsAsFactors = FALSE)

# Make sure X is numeric (BioMart expects numeric for Entrez)
deg_df$X <- as.numeric(deg_df$X)

# Connect to Ensembl
mart <- useEnsembl(
  biomart = "genes",
  dataset = "hsapiens_gene_ensembl",
  mirror = "useast"
)

# Get mapping using Entrez IDs
mapping <- getBM(
  attributes = c("entrezgene_id", "ensembl_gene_id", "hgnc_symbol"),
  filters = "entrezgene_id",
  values = deg_df$X,
  mart = mart
)

mapping_clean <- mapping %>%
  filter(!is.na(ensembl_gene_id)) %>%
  group_by(entrezgene_id) %>%
  filter(n() == 1) %>%   # keep only uniquely mapped Entrez IDs
  ungroup()

# Join back to your dataframe
deg_df_merged <- deg_df %>%
  left_join(mapping_clean, by = c("X" = "entrezgene_id"))

# Filter to successfully mapped genes
deg_df_merged <- deg_df_merged %>% dplyr::filter(!is.na(ensembl_gene_id))

# Filter the patient data to just harmonized Diagnosis score in the DLPFC and the AD-CONTROL comparison
rosmap_filtered <- rosmap_df %>%
  filter(
    Comparison == "AD-CONTROL",
    Model == "Diagnosis",
    Tissue == "DLPFC"
  )

# Step 1: standardize column names
modules_df <- modules_df %>%
  dplyr::rename(ensembl_gene_id = `GeneID`)

# Step 2: merge DEG + Modules
deg_modules <- deg_df_merged %>%
  inner_join(modules_df, by = "ensembl_gene_id")

# Step 3: merge the above with filtered ROSMAP
final_merged <- deg_modules %>%
  inner_join(rosmap_filtered, by = "ensembl_gene_id")

# Compute correlation for each Module
module_correlations <- final_merged %>%
  group_by(Module) %>%   # adjust this if your module column has a different name
  summarise(
    cor_result = list(cor.test(log2FoldChange, logFC)),
    .groups = "drop"
  ) %>%
  mutate(
    cor_estimate = map_dbl(cor_result, ~ .x$estimate),
    p_value     = map_dbl(cor_result, ~ .x$p.value)
  ) %>%
  dplyr::select(Module, cor_estimate, p_value)


module_correlations_filtered <- final_merged %>%
  group_by(Module) %>%
  summarise(
    cor_result = list(
      cor.test(log2FoldChange, logFC, use = "complete.obs")
    ),
    n_pairs = sum(!is.na(log2FoldChange) & !is.na(logFC)),
    .groups = "drop"
  ) %>%
  mutate(
    cor_estimate = map_dbl(cor_result, ~ .x$estimate),
    p_value     = map_dbl(cor_result, ~ .x$p.value)
  ) %>%
  dplyr::select(Module, cor_estimate, p_value, n_pairs)

cluster_lookup <- c(
  # Cluster A: ECM organization
  "TCXblue" = "Consensus Cluster A (ECM Organization)",
  "PHGyellow" = "Consensus Cluster A (ECM Organization)",
  "IFGyellow" = "Consensus Cluster A (ECM Organization)",
  
  # Cluster B: Immune system
  "DLPFCblue" = "Consensus Cluster B (Immune system)",
  "CBEturquoise" = "Consensus Cluster B (Immune system)",
  "IFGturquoise" = "Consensus Cluster B (Immune system)",
  "FPturquoise" = "Consensus Cluster B (Immune system)",
  "PHGturquoise" = "Consensus Cluster B (Immune system)",
  "TCXturquoise" = "Consensus Cluster B (Immune system)",
  "STGblue" = "Consensus Cluster B (Immune system)",
  
  # Cluster C: Neuronal system
  "IFGbrown" = "Consensus Cluster C (Neuronal system)",
  "STGbrown" = "Consensus Cluster C (Neuronal system)",
  "DLPFCyellow" = "Consensus Cluster C (Neuronal system)",
  "TCXgreen" = "Consensus Cluster C (Neuronal system)",
  "FPyellow" = "Consensus Cluster C (Neuronal system)",
  "CBEyellow" = "Consensus Cluster C (Neuronal system)",
  "PHGbrown" = "Consensus Cluster C (Neuronal system)",
  
  # Cluster D: Cell Cycle, NMD
  "DLPFCbrown" = "Consensus Cluster D (Cell Cycle, NMD)",
  "STGyellow" = "Consensus Cluster D (Cell Cycle, NMD)",
  "PHGgreen" = "Consensus Cluster D (Cell Cycle, NMD)",
  "CBEbrown" = "Consensus Cluster D (Cell Cycle, NMD)",
  "TCXyellow" = "Consensus Cluster D (Cell Cycle, NMD)",
  "IFGblue" = "Consensus Cluster D (Cell Cycle, NMD)",
  "FPblue" = "Consensus Cluster D (Cell Cycle, NMD)",
  
  # Cluster E: Organelle Biogenesis, Cellular stress response
  "FPbrown" = "Consensus Cluster E (Organelle Biogenesis, Cellular Stress Response)",
  "CBEblue" = "Consensus Cluster E (Organelle Biogenesis, Cellular Stress Response)",
  "DLPFCturquoise" = "Consensus Cluster E (Organelle Biogenesis, Cellular Stress Response)",
  "TCXbrown" = "Consensus Cluster E (Organelle Biogenesis, Cellular Stress Response)",
  "STGturquoise" = "Consensus Cluster E (Organelle Biogenesis, Cellular Stress Response)",
  "PHGblue" = "Consensus Cluster E (Organelle Biogenesis, Cellular Stress Response)"
)

module_correlations <- module_correlations %>%
  mutate(Consensus_Cluster = cluster_lookup[Module])

# Ensure cluster_colors vector exists
cluster_colors <- c(
  "Consensus Cluster A (ECM Organization)" = "orange",
  "Consensus Cluster B (Immune system)" = "purple",
  "Consensus Cluster C (Neuronal system)" = "green",
  "Consensus Cluster D (Cell Cycle, NMD)" = "brown",
  "Consensus Cluster E (Organelle Biogenesis, Cellular Stress Response)" = "blue"
)


# Scatter plot with Prism-style aesthetics
ggplot(module_correlations, aes(
    x = cor_estimate,
    y = -log10(p_value),
    label = Module,
    color = Consensus_Cluster
  )) +
  geom_point(size = 3, alpha = 0.8) +
  geom_text_repel(
    size = 3,
    max.overlaps = 20,
    box.padding = 0.3,
    point.padding = 0.5
  ) +
  geom_hline(yintercept = -log10(0.05), linetype = "dashed", color = "black") +
  geom_vline(xintercept = 0, linetype = "dashed", color = "black") +
  scale_color_manual(values = cluster_colors) +
  labs(
    x = "Correlation Coefficient (Organoid Model vs. AD DLPFC)",
    y = "-log10(p-value)",
    title = "Module-wise Correlation between Organoid Model vs. Patient Tissue",
    color = "Consensus Cluster"
  ) +
  theme(
    # White background, minimal grid
    panel.background = element_rect(fill = "white", color = NA),
    panel.border = element_blank(),
    panel.grid.major = element_blank(),
    panel.grid.minor = element_blank(),
    
    # Axis lines on bottom and left only
    axis.line = element_line(color = "black"),
    
    # Text
    text = element_text(size = 12),
    plot.title = element_text(hjust = 0.5, face = "bold"),
    axis.title = element_text(face = "bold"),
    
    # Legend
    legend.background = element_rect(fill = "white", color = NA),
    legend.key = element_rect(fill = "white", color = NA),
    legend.text = element_text(size = 10),
    legend.title = element_text(face = "bold")
  ) +
  xlim(-0.25, 0.25)

# Inspect modules (not presented in manuscript)
# --- Step 1: read CSV ---
file_path <- "GO_results/P301L,V337M DOX_vs_P301L,V337M_GO_UP_BP_simplified.csv"
go_df <- read_csv(file_path)

# --- Step 2: split geneID into a list column ---
go_df <- go_df %>%
  mutate(gene_list = strsplit(geneID, "/"))

# --- Step 3: map HGNC symbols to Ensembl IDs ---
mart <- useEnsembl(biomart = "genes", dataset = "hsapiens_gene_ensembl", mirror = "useast")

# Flatten all gene symbols to map at once
all_genes <- unique(unlist(go_df$gene_list))

symbol_map <- getBM(
  attributes = c("hgnc_symbol", "ensembl_gene_id"),
  filters = "hgnc_symbol",
  values = all_genes,
  mart = mart
)

# --- Step 4: add Ensembl IDs to each pathway ---
go_df <- go_df %>%
  mutate(
    ensembl_list = map(gene_list, ~ symbol_map$ensembl_gene_id[match(.x, symbol_map$hgnc_symbol)])
  )

# --- Step 5: count genes per module ---
# Make a vector of unique module names
module_names <- unique(modules_df$Module)

# Function to count how many genes fall into each module
count_modules <- function(genes) {
  sapply(module_names, function(mod) {
    sum(genes %in% modules_df$ensembl_gene_id[modules_df$Module == mod], na.rm = TRUE)
  })
}

# Apply to each row
module_counts <- map_dfr(go_df$ensembl_list, count_modules)

# Name the columns by module
colnames(module_counts) <- module_names

# --- Step 6: combine counts with original data ---
go_df_final <- bind_cols(go_df, module_counts)


# Reshape to long format, avoiding name collision
heatmap_df <- go_df_final %>%
  dplyr::select(-gene_list, -ensembl_list) %>%  # remove list columns
  pivot_longer(
    cols = all_of(module_names),
    names_to = "Module",
    values_to = "Gene_Count"   # use a unique name
  )

# Optional: reorder pathways by total mapped genes
pathway_order <- heatmap_df %>%
  group_by(pathway = Description) %>%  # replace Description with your pathway column name
  summarise(total_genes = sum(Gene_Count)) %>%
  arrange(desc(total_genes)) %>%
  pull(pathway)

heatmap_df$Description <- factor(heatmap_df$Description, levels = pathway_order)

# Heatmap plot
ggplot(heatmap_df, aes(x = Module, y = Description, fill = Gene_Count)) +
  geom_tile(color = "white") +
  scale_fill_gradient(low = "white", high = "steelblue") +
  theme_minimal() +
  labs(
    x = "Module",
    y = "Pathway",
    fill = "Gene Count",
    title = "Module representation in each GO pathway"
  ) +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1),
    axis.title = element_text(face = "bold"),
    plot.title = element_text(hjust = 0.5, face = "bold")
  )


# Option 1: remove list columns entirely
write.csv(
  go_df_final %>% dplyr::select(-gene_list, -ensembl_list),
  "2026.03.24_module_makeup.csv",
  row.names = FALSE
)

# Option 2: convert lists to strings so they can be saved
go_df_export <- go_df_final %>%
  mutate(
    gene_list = map_chr(gene_list, ~ paste(.x, collapse = "/")),
    ensembl_list = map_chr(ensembl_list, ~ paste(.x, collapse = "/"))
  )

write.csv(go_df_export, "2026.03.24_module_makeup.csv", row.names = FALSE)


# Compute module-level averages
module_averages <- final_merged %>%
  group_by(Module) %>%
  summarise(
    avg_log2FoldChange = mean(log2FoldChange, na.rm = TRUE),
    avg_logFC         = mean(logFC, na.rm = TRUE),
    .groups = "drop"
  ) %>%
  mutate(
    Consensus_Cluster = cluster_lookup[Module]
  )

# Scatter plot with colors for consensus cluster
ggplot(module_averages, aes(
    x = avg_log2FoldChange,
    y = avg_logFC,
    label = Module,
    color = Consensus_Cluster
  )) +
  geom_point(size = 3, alpha = 0.8) +
  geom_text_repel(
    size = 3,
    max.overlaps = 20,
    box.padding = 0.3,
    point.padding = 0.5
  ) +
  geom_hline(yintercept = 0, linetype = "dashed", color = "black") +
  geom_vline(xintercept = 0, linetype = "dashed", color = "black") +
  scale_color_manual(values = cluster_colors) +
  labs(
    x = "Average log2FoldChange (Organoid Model)",
    y = "Average logFC (ROSMAP AD DLPFC)",
    title = "Module-wise Average Expression Comparison",
    color = "Consensus Cluster"
  ) +
  theme(
    # White background, minimal grid
    panel.background = element_rect(fill = "white", color = NA),
    panel.border = element_blank(),
    panel.grid.major = element_blank(),
    panel.grid.minor = element_blank(),
    
    # Axis lines on bottom and left only
    axis.line = element_line(color = "black"),
    
    # Text
    text = element_text(size = 12),
    plot.title = element_text(hjust = 0.5, face = "bold"),
    axis.title = element_text(face = "bold"),
    
    # Legend
    legend.background = element_rect(fill = "white", color = NA),
    legend.key = element_rect(fill = "white", color = NA),
    legend.text = element_text(size = 10),
    legend.title = element_text(face = "bold")
  )


# Compute module-level averages and IQRs
module_stats <- final_merged %>%
  group_by(Module) %>%
  summarise(
    avg_log2FoldChange = mean(log2FoldChange, na.rm = TRUE),
    avg_logFC         = mean(logFC, na.rm = TRUE),
    log2FC_Q1 = quantile(log2FoldChange, 0.25, na.rm = TRUE),
    log2FC_Q3 = quantile(log2FoldChange, 0.75, na.rm = TRUE),
    logFC_Q1  = quantile(logFC, 0.25, na.rm = TRUE),
    logFC_Q3  = quantile(logFC, 0.75, na.rm = TRUE),
    .groups = "drop"
  ) %>%
  mutate(
    Consensus_Cluster = cluster_lookup[Module]
  )

# Scatter plot
fc_p <- ggplot(module_stats, aes(
    x = avg_log2FoldChange,
    y = avg_logFC,
    label = Module,
    color = Consensus_Cluster
  )) +
  # Error bars (vertical = logFC, horizontal = log2FoldChange)
 # geom_errorbar(aes(ymin = logFC_Q1, ymax = logFC_Q3), width = 0, alpha = 0.6) +
  #geom_errorbarh(aes(xmin = log2FC_Q1, xmax = log2FC_Q3), width = 0, alpha = 0.6) +
  
  # Points
  geom_point(size = 2.5, alpha = 0.8) +
  
  # Labels
  geom_text_repel(
    size = 1.5,
    max.overlaps = Inf,
    box.padding = 0.3,
  point.padding = 0.5
  ) +
  
  # Reference lines
  geom_hline(yintercept = 0, linetype = "dashed", color = "black") +
  geom_hline(yintercept = 0.5, linetype = "dashed", color = "grey") +
  geom_hline(yintercept = -0.5, linetype = "dashed", color = "grey") +
  geom_vline(xintercept = 0, linetype = "dashed", color = "black") +
  
  # Color by cluster
  scale_color_manual(values = cluster_colors) +
  
  # Labels & theme
  labs(
    x = "Average log2 Fold Change \n Organoid Model",
    y = "Average log2 Fold Change \n AD DLPFC",
    title = "Module-wise Average Expression with IQR",
    color = "Consensus Cluster"
  ) +
  theme(
    panel.background = element_rect(fill = "white", color = NA),
    panel.border = element_blank(),
    panel.grid.major = element_blank(),
    panel.grid.minor = element_blank(),
    axis.line = element_line(color = "black"),
    text = element_text(size = 12),
    #plot.title = element_text(hjust = 0.5, face = "bold"),
    #axis.title = element_text(face = "bold"),
    #legend.background = element_rect(fill = "white", color = NA),
    #legend.key = element_rect(fill = "white", color = NA),
    #legend.text = element_text(size = 10),
    #legend.title = element_text(face = "bold")
    legend.position = 'none'
  ) +
  xlim(-0.10, 0.10) +
  ylim(-0.10, 0.10)

ggsave("fold_change_plot_no_labels.png", plot = fc_p, width = 3, height = 3.5, dpi = 900)


### PLOT TRANSCRIPTS PER CELL TYPE USING HPA DATA ---
# File path; available at https://www.proteinatlas.org/humanproteome/single+cell/single+cell+type/data#datasets
tsv_file <- "rna_single_nuclei_cluster_type.tsv"

# Explicit gene order (top → bottom)
gene_order <- c(
  "MAPT", "GREB1L","TBC1D1", "RERGL", "CFAP61", 
  "ONECUT1", "AKAIN1",
  "VGLL3", "FGF9",  "GFAP",
  "S100B", "AQP4", "DAAM2", "TIMP4"
)

# Explicit cell population order (left → right)
cluster_order <- c(
  "amygdala excitatory",
  "cerebellar inhibitory",
  "deep-layer corticothalamic and 6b",
  "deep-layer intratelencephalic",
  "deep-layer near-projecting",
  "eccentric medium spiny neuron",
  "hippocamal CA1-3",
  "hippocampal CA4",
  "hippocampal dentate gyrus",
  "LAMP5-LHX6 and chandelier",
  "lower rhombic lip",
  "mammillary body",
  "medium spiny neuron",
  "MGE interneuron",
  "midbrain-derived inhibitory",
  "miscellaneous",
  "splatter",
  "thalamic excitatory",
  "upper rhombic lip",
  "upper-layer intratelencephalic",
  "astrocyte",
  "Bergmann glia",
  "central nervous system macrophage",
  "committed oligodendrocyte precursor",
  "oligodendrocyte",
  "oligodendrocyte precursor cell",
  "ependymal cell",
  "choroid plexus epithelial cell",
  "endothelial cell",
  "pericyte",
  "vascular associated smooth muscle cell",
  "fibroblast",
  "leukocyte"
)

# Cell population groups for coloring
cluster_groups <- tibble(
  `Cluster type` = cluster_order,
  cell_class = c(
    # Neural (gold)
    rep("Neural", 20),

    # Glial (brown)
    "Glial",  # astrocyte
    "Glial",  # Bergmann glia
    "Glial",  # CNS macrophage
    "Glial",  # committed OPC
    "Glial",  # oligodendrocyte
    "Glial",  # OPC

    # Ciliated (green)
    "Ciliated", # ependymal
    "Ciliated", # choroid plexus

    # Mural (purple)
    "Mural", # endothelial
    "Mural", # pericyte
    "Mural", # vascular smooth muscle

    # Other
    "Mesenchymal",
    "Immune"
  )
)

# Read TSV
rna_df <- readr::read_tsv(tsv_file, col_types = cols())

# Filter and enforce factor levels
plot_df <- rna_df %>%
  filter(
    `Gene name` %in% gene_order,
    `Cluster type` %in% cluster_order
  ) %>%
  left_join(cluster_groups, by = "Cluster type") %>%
  mutate(
    Gene = factor(`Gene name`, levels = gene_order),
    `Cluster type` = factor(`Cluster type`, levels = cluster_order),
    cell_class = factor(
      cell_class,
      levels = c("Neural", "Glial", "Ciliated", "Mural", "Mesenchymal", "Immune")
    )
  )

# Dot plot
dotPlot_plot <- ggplot(plot_df, aes(
  x = `Cluster type`,
  y = `Gene name`,
  size = nCPM,
  fill = cell_class
)) +
  geom_point(
    shape = 21,
    color = "black",
    stroke = 0.4,
    alpha = 0.9
  ) +
  scale_size_continuous(
    name = "nCPM",
    range = c(0.5, 6)
  ) +
  scale_fill_manual(
    name = "Cell class",
    values = c(
      Neural = "#D4AF37",
      Glial = "#8B5A2B",
      Ciliated = "#2E8B57",
      Mural = "#6A0DAD",
      Mesenchymal = "#E67E22",
      Immune = "#C0392B"
    )
  ) +
  labs(
    x = "Cell population",
    y = "Gene",
    title = "Single-nuclei RNA expression across brain cell populations"
  ) +
  theme_bw() +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1, vjust = 1),
    panel.grid.major = element_line(color = "grey90"),
    panel.grid.minor = element_blank()
  ) +
  scale_y_discrete(limits = rev(gene_order))

ggsave("HPA_expression.png", plot = dotPlot_plot, width = 7, height = 4.5, dpi = 300)
